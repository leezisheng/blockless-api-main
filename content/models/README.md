# content/models/

License-cleared 3D component assets (`.glb` / `.obj`) served by `/v1/models`.

Each servable asset is declared in the recommendation registry with an
attribution string and an `http(s)` source URL; the route fails closed on any
asset missing verified provenance. No binary assets are bundled in this snapshot.
